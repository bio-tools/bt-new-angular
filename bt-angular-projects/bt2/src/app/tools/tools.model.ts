export class Tools {
    count: number;
    previous: string;
    next: string;
    // this needs to be improved to actually model everything for a tool
    // or just have a type of Tool[], where we define what is a tool
    list: any[];
}
